Guide Updates JSON generation rules:
- Always use canonical schema.
- Do not invent fields.
- Follow example JSON files.
- If data is missing, use null or empty arrays.
- Maintain backward compatibility.
- Increase schemaVersion only for intentional schema evolution.
- Swift Codable models must remain compatible with schema examples.
